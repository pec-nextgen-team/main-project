\# Git Training Practice



\## Git Workflow



1\. Created a personal Git branch:

&#x20;  `training/pec-nextgen-team-git-practice`



2\. Created my personal training file:

&#x20;  `docs/git-training/pec-nextgen-team.md`



3\. Practiced checking the repository status using:

&#x20;  `git status`



4\. Practiced checking available branches using:

&#x20;  `git branch -a`



5\. Practiced creating and switching to a new branch using:

&#x20;  `git switch -c`



6\. Practiced the Git workflow of creating, committing, and pushing changes.



\## Key Git Commands Learned



\- `git status` — Check the current repository status.

\- `git branch -a` — View local and remote branches.

\- `git switch -c <branch-name>` — Create and switch to a new branch.

\- `git add <file>` — Stage a file for commit.

\- `git commit -m "<message>"` — Commit changes.

\- `git push` — Push changes to GitHub.



\## Learning



This practice helped me understand the basic Git and GitHub workflow, including creating a branch, making a change, committing it, pushing it to GitHub, and creating a Pull Request.

